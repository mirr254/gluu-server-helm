kind: PersistentVolumeClaim
apiVersion: v1
metadata: 
  name: {{ template "gluu.fullname" . }}-init-pvc
spec:
  storageClassName: {{ template "gluu.fullname" . }}-gluu-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.volumeclaim.storage }}
      