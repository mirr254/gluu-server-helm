apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ template "gluu.fullname" . }}-storage-class
  labels:
    app: {{ template "gluu.fullname" . }}
  annotations:
    storageclass.beta.kubernetes.io/is-default-class: "false"
provisioner: {{ .Values.provisioner }}
parameters:
  {{- if eq .Values.provisioner "kubernetes.io/aws-ebs" }}
  type: gp2
  encrypted: "true"
  zones: {{ .Values.zones }}
  description: {{ .Values.description }}
  {{- else if eq .Values.provisioner "kubernetes.io/gce-pd" }}
  type: {{ .Values.gcePdStorageType }}
  description: {{ .Values.description }}
  {{- else if eq .Values.provisioner "kubernetes.io/azure-disk" }}
  storageAccountType: {{ .Values.azureStorageAccountType }}
  kind: {{ .Values.azureStorageKind }}
  {{- else }}
  pool: {{ .Values.pool }}
  description: {{ .Values.description }}
  fsType: {{ .Values.fsType }}
  {{- end }}
allowVolumeExpansion: true
reclaimPolicy: {{ .Values.reclaimPolicy }}
mountOptions:
  - debug
  
