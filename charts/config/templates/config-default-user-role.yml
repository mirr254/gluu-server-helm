kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ template "gluu.fullname" . }}-role
rules:
- apiGroups: [""]
  resources: ["configMaps", "secrets"]
  verbs: ["get","list", "watch", "create", "update", "patch","delete"]