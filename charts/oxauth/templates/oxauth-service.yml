apiVersion: v1
kind: Service
metadata:
  name: {{ template "gluu.fullname" . }}-svc
  labels:
    app: {{ template "gluu.fullname" . }}
spec:
  ports:
  - port: {{ .Values.ports.containerPort }}
    name: {{ template "gluu.fullname" . }}
  selector:
    app: {{ template "gluu.fullname" . }}